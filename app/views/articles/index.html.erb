  <%= link_to "New Article", new_article_path %>


<% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <%= link_to "Delete", article_path(article),
  method: :delete,
  data: { confirm: "Really?  That's a great article!"} %>

  <p><%= article.content %></p>
<% end %>
